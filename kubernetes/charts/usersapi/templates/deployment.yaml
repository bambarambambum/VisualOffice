---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "usersapi.fullname" . }}
  labels:
    app: visualoffice
    component: usersapi
    release: {{ .Release.Name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: visualoffice
      component: usersapi
      release: {{ .Release.Name }}
  template:
    metadata:
      name: usersapi-pod
      labels:
        app: visualoffice
        component: usersapi
        release: {{ .Release.Name }}
    spec:
      containers:
      - image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        name: usersapi
        env:
        - name: MYSQL_SERVER_NAME
          value: {{ .Values.databaseHost | default (printf "%s-mysql" .Release.Name)}} #mysql
        - name: MYSQL_USER
          value: {{ .Values.databaseUser}} #visualoffice
        - name: MYSQL_USER_PASSWORD
          value: {{ .Values.databaseUserPassword}} #VeryStrongPassword
        - name: MYSQL_DATABASE
          value: {{ .Values.databaseName}} #visualoffice